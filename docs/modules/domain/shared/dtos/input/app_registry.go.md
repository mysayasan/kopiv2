# Module: domain/shared/dtos/input/app_registry.go

## Purpose

Input DTO for app registry create/update payloads.

## Notes

- Mirrors `entities.AppRegistry`.
- Includes `clientSecret` for write requests.
