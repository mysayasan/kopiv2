# Local secrets — real target + credentials
config/target.env
config/myseliasan.target.env
config/myiotsan.target.env
config/myidsan.target.env

# Scan output
reports/*
!reports/.gitkeep

# Plans rendered per-run with the Basic-auth header baked in (see scan.sh).
# Contain a live credential — never commit.
plans/.rendered-*
